# T30 — Log de la revisión continua (2026-07-14)

Registro operativo, no un plan: un resumen corto de cada tarea que se hizo hoy (sesión completa,
incluidas las de antes del `/loop`) — qué se encontró, cómo se verificó, y el commit. Las ideas
sin implementar viven en `tareas/29-ideas-futuras.md`, no aquí.

## Antes del loop (probando en teléfono/emulador con el dueño)

| # | Qué | Cómo se encontró / verificó | Commit |
|---|-----|------------------------------|--------|
| 1 | Tienda: 4,9 s → 11 ms | Instrumentado con cronómetro real (no adivinado); el costo era `Categories.Clear()+N Add()` sobre un `CollectionView`, no la red | `800aecc` |
| 2 | Diorama: picos de GC de 25-30 ms → 11-18 ms | Medido antes/después; la media de pintado (8 ms) nunca fue el problema, eran objetos `SKPaint`/blur creados por frame | `ee2b592` |
| 3 | Mascota borrosa/levitando/con emoji | La mascota vivía fuera del lienzo (`<Image>` XAML); movida dentro de `RoomDiorama`, anclada a su sombra, orden de profundidad isométrico | `7068a9b` |
| 4 | `/health` no aceptaba `HEAD` → 10 días de falsa alerta de caída | UptimeRobot manda HEAD; el endpoint era `MapGet`. Verificado con `curl -I` antes y después | `b444207` |
| 5 | Sentry en el cliente (crashes invisibles antes) | Smoke test real: `SentrySdk.CaptureMessage` de prueba, confirmado en el dashboard, luego retirado | `f55a894` |
| 6 | Economía del foco pagaba menos que mentir (foco 5 min: 10 XP; "leer" a mano: 20 XP) | Reproducido con los números exactos que reportó el dueño; decisión suya sobre el fix (piso ×1.4 + juez más estricto) | `24fd990` |
| 7 | Permisos de foco no se refrescaban / foco "se olvidaba" activo / notificación no reabría la pantalla / hueco fantasma de la mascota en la grilla | 4 bugs de una sola sesión de prueba en teléfono real; cada uno con su causa raíz (resume de Activity vs. navegación de Shell; falta de chequeo `IsActive`; falta el marcador `ActionOpenFocus`; regla de la baldosa (3,3) sin aviso) | `e12a279` |
| 8 | T20-I1b: `ToggleRitualCell` con string mágico "User not found" | Ya estaba en el toll-list de `tareas/20-consistencia-menores.md`, solo había que ejecutarlo | (incluido en la ronda de arriba) |

## Iteraciones del `/loop` (autónomo, dueño ausente)

| # | Área elegida | Qué se encontró | Cómo se verificó | Resultado |
|---|---|---|---|---|
| 1 | `GroupsController`/`GroupService` | `UserId` fantasma en 5 endpoints (create/join/mine/approve/leave) — el server siempre usa el token, el campo/parámetro viajaba y se ignoraba. Mismo patrón que T18 ya mató en otros controllers | Grep de usos en cliente y server antes de tocar; build limpio cliente+servidor | Arreglado, `4e03ad1` |
| 2 | Foco grupal (`FocusController.JoinInternal`) | Camino real (no hipotético) para cobrar recompensa de foco grupal sin esperar: una `FocusSession` SOLA abandonada sobre la mascota compartida (alcanzable desde "Registrar tarea" → Foco sin pasar por "Foco grupal") se reutilizaba sin realinear su reloj | Test de regresión nuevo (`GroupFocusJoinTests`), verificado **en ambas direcciones**: revertido el fix a mano → el test falla; restaurado → pasa | Arreglado, `4b3ff59` |
| 3 | `AuthController`/`SessionController` (refresh tokens) | Un comentario prometía "cascada de revocación" al reusar un token ya revocado — **nunca se implementó**. No es un bug mecánico, es una capa de seguridad real que falta | Grep exhaustivo: 0 tests, 0 código, solo el comentario | Comentario corregido (`1c8fb22`); diseño completo de la implementación real dejado en T29 para que el dueño lo revise antes de construirla — no se implementó solo por tocar auth |
| 4 | `TasksController`/`PetService.ProcessTaskCompletion`/`ApproveTaskAsync` | Nada — chequeo de membresía, compuerta de relevancia, mayoría de votos, auto-aprobación y reenvío del cliente, todo correcto | Lectura completa de las 3 capas (controller, servicio, cliente) | Sin hallazgos |
| 5 | `FamilyHub`/`PresenceService`/`RealtimeService` (SignalR, presencia, Frenesí) | Nada — locks consistentes, umbral de Frenesí (≥2) igual en todos lados, multi-conexión bien manejada, reconexión resincroniza (T25), `PetUpdate` verificado extremo a extremo (no es un evento fantasma) | Lectura completa server+cliente, grep de `"PetUpdate"` para confirmar que el emisor existe | Sin hallazgos |
| 6 | `PushService` (FCM) | Un token de FCM caducado (usuario reinstaló/cambió de teléfono) se reintentaba PARA SIEMPRE — el catch genérico no distinguía "fallo transitorio" de "FCM confirma que ya no existe" | Revisado el XML del paquete `FirebaseAdmin` instalado (3.1.0) para confirmar que existe `MessagingErrorCode.Unregistered`; confirmado que el cliente se reregistra solo (`PushRegistration.RegisterAsync`), así que limpiar es autosanable | Arreglado, `096634e`. **Sin test de regresión**: el SDK no es mockable sin envolverlo en una interfaz nueva — desproporcionado para un hallazgo de higiene de datos, no de seguridad/economía |
| 7 | `AccountService.DeleteAsync` + `GroupService.LeaveGroupAsync` (borrado de cuenta, T14-C1) | Nada — sorprendentemente completo: voto fantasma, solicitudes huérfanas, afecto/votos de nacimiento residuales, unanimidad pasiva al achicarse el grupo, todo ya cubierto por T24. Las fotos de comprobante son bytea en la misma fila (sin blob storage externo que quede huérfano) | Cruce de los 12 `DbSet` de `AppDbContext` contra lo que limpia `DeleteAsync`, y lectura completa de `LeaveGroupAsync` (90 líneas) para confirmar que no duplica ni deja huecos con el borrado de cuenta | Sin hallazgos |
| 8 | `ShopController.BuyItem`/`EquipStyle` (código) + emulador navegando "Nueva familia" | Compra/equipar: sin hallazgos (precio siempre del catálogo del servidor, lock+reload anti doble-gasto, `Heal()` ya bloquea curar una mascota cristalizada, `BuyRequest.UserId` fantasma de T18 sigue muerto). **Encontrado a simple vista en el emulador**: el picker de arquetipo en "Nueva familia" mostraba "Estudio" en español en medio de una pantalla en inglés — `CreateGroupViewModel.Archetypes` nunca pasaba por `L.T()` | Screenshot antes/después: reconstruido el cliente, navegado a la pantalla con el sistema en inglés, confirmado que ahora dice "Study" | Arreglado, `bdc2e73` |
| 9 | Ajustes → Acerca de → Credits (emulador) | Ajustes en sí, ya bien traducido (validó el fix de permisos de hoy). **La fila "Credits" tenía el chevron de navegación pero ningún `TapGestureRecognizer`** — parecía interactiva y no hacía nada. De paso: 0 créditos a Bongseng en la app (solo en comentarios de código) | Tap real en el emulador (sin efecto, confirmó el bug); tras el fix, tap de nuevo y el popup aparece con el crédito | Arreglado, `26993af` |
| 10 | `BirthCeremonyViewModel` (la PRIMERA pantalla que ve todo usuario nuevo) | 🔴 **El peor bug del loop hasta ahora.** `NavigateToLogin` marcaba `HasHatched=true` al NAVEGAR al login, no al autenticarse. Si el usuario tocaba "¿Ya tienes cuenta?" y no llegaba a iniciar sesión (se arrepiente, falla la contraseña, se cae la red, cierra la app), al reabrir se saltaba la ceremonia **para siempre**: aterrizaba en el Dashboard con una mascota invitada llamada "Huevo", de especie aleatoria, que nunca eligió ni nombró | **Reproducido en el emulador** (datos limpios → ceremonia → "Log in" → matar app → reabrir → mascota "Huevo"). Fix verificado en ambas direcciones: mismo camino → la ceremonia sobrevive; y camino feliz intacto (nombre "Verifica" conservado, sin repetir ceremonia) | Arreglado, `8a0f3d8`. `HasHatched` se marca ahora en `GameDataService.SetUser` — único punto por el que pasan los 4 caminos de auth exitosa, así que ninguno futuro puede olvidarlo |
| 11 | `RegisterViewModel` / registro directo (siguiendo el rastro de la iteración 10) | Registrarse SIN pasar por invitado (ceremonia → "¿Ya tienes cuenta?" → "Regístrate") creaba la mascota con `Name + "'s Pet"` — genitivo sajón **hardcodeado en inglés**, saltándose el sistema `L`. Contradice la política T16 (todo texto visible nace en español) | Reproducido en el emulador con la app en ESPAÑOL: mascota "Ana's Pet" en medio de una pantalla toda en español. Fix verificado en AMBOS idiomas: es→"Mascota de Ana", en→"Ana's Pet" | Arreglado, `4632ee7`. Que este usuario no pueda ELEGIR el nombre es decisión de diseño → anotada en T29, no implementada |
| 12 | `AuthService`/`AuthController`/`SessionController`/`UsersController.UpgradeAccount` (login con Google, upgrade de invitado, cancelación) | Revisión de fondo del hilo completo: `UpgradeAccount` no reasigna especie/arquetipo (correcto, la mascota del invitado ya existe); `Callback` de Google solo asciende al invitado si el email es nuevo (protegido con `.StartsWith("guest_")`, no hay forma de "robar" una cuenta ajena); `GoogleCodes` (código de un solo uso, 2 min, limpieza oportunista) sin problemas; cancelar el login de Google (`TaskCanceledException`) ya se maneja bien. Único hallazgo real, menor: el fallback de navegación en `LoginViewModel.NavigateToDashboard` mostraba `"Nav Error"` / `"Ruta fallida: ..."` **hardcodeado en inglés**, saltándose `L` — mismo patrón de la iteración 11, esta vez en un camino defensivo casi inalcanzable | Grep de todo el hilo (4 caminos de auth exitosa, los 3 controllers server-side) antes de tocar nada; build limpio del cliente tras el fix (0 errores) | Arreglado (localizado con `L.T`/`L.F`), sin commit todavía. El resto del hilo: sin hallazgos — merge de cuentas al hacer login/Google con una cuenta YA existente (mientras había un invitado con progreso) es NO-merge por diseño (estándar en apps con auth), no un bug |

| 13 | `StatsPage`/`DashboardPage` (chip de nivel) | El chip superior del Dashboard mostraba `"Nv {TotalXp/1000+1}"` — un "nivel" inventado, calculado aparte y desconectado de la etapa REAL de la mascota (Huevo/Cría/Adulto/Maestro) que el subtítulo, 2 líneas más abajo, en la MISMA pantalla, ya muestra (`"Ember · Maestro"`). Una mascota en etapa Maestro podía leer "Nv 3" arriba y "Maestro" abajo a la vez. `StatsPage` repetía el mismo cálculo con su propio nombre (`CurrentLevel`/`LevelProgress`), más un campo `XpToNextLevel` declarado, inicializado y nunca leído por nadie (ni bindeado en XAML) | Grep de `CurrentLevel`/`LevelLabel`/`XpToNextLevel` confirmó que el "nivel" no se usa en ningún otro lado del cliente — puramente decorativo y huérfano del sistema de etapas real. Unificado: `PetVisuals.StageName`/`StageProgress` (nuevos, reusan los umbrales ya existentes en `PetEvolution`) son ahora la única fuente para Dashboard Y Stats; se eliminó el switch duplicado de `DashboardViewModel` y el "Nivel" de `StatsViewModel`. Build cliente limpio (0 errores) antes y después | Arreglado, `3cdefcd` |

## Convención de este log

Una fila por tarea/iteración, no por commit — si una iteración no encontró nada, se anota igual
("sin hallazgos" es información real). Se actualiza al cerrar cada iteración del loop.
